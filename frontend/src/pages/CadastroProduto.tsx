import { Header } from '@/components/layout/Header'

// TODO: tela em construção — formulário multi-etapa (categoria, atributos,
// imagens, variações) ainda não foi montado.
export function CadastroProduto() {
  return (
    <div className="min-h-screen bg-base">
      <Header />
      <main className="p-6">
        <p className="text-sm text-ink-soft">Cadastro de produto — em construção.</p>
      </main>
    </div>
  )
}
