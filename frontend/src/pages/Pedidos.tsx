import { Header } from '@/components/layout/Header'

// TODO: tela em construção — ainda falta o cliente de API pra GET /orders.
export function Pedidos() {
  return (
    <div className="min-h-screen bg-base">
      <Header />
      <main className="p-6">
        <p className="text-sm text-ink-soft">Pedidos — em construção.</p>
      </main>
    </div>
  )
}
